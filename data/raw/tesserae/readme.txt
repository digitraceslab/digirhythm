pa_on_off: phone on/off events only for Android

pa_phone_locked: screen on/off if agent = iOS, and lock/unlock if agent=Androind, event though it always says lock/unlock

call_logs: call events, no texts
